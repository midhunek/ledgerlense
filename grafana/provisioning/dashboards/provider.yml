apiVersion: 1

providers:
  - name: LedgerLens
    orgId: 1
    folder: LedgerLens
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
